import React from 'react';
import { IncomeRecordCardProps } from '../types';

export default function IncomeRecordCard({ record }: IncomeRecordCardProps): JSX.Element {
    return (
        <div>
            Income Record Card
        </div>
    )
}