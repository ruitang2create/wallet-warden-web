import React from 'react';
import { SpendingRecordCardProps } from '../types';

export default function SpendingRecordCard({ record }: SpendingRecordCardProps): JSX.Element {
    return (
        <div>
            Spending Record Card
        </div>
    )
}